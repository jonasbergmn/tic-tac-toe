[project]
name = "python-helpers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.4",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
]